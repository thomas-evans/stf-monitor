import { TitleCleanupPipe } from './title-cleanup.pipe';

describe('TitleCleanupPipe', () => {
  it('create an instance', () => {
    const pipe = new TitleCleanupPipe();
    expect(pipe).toBeTruthy();
  });
});
